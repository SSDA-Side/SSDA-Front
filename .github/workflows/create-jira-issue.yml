name: Create Jira issue
on:
  issues:
    types:
      - opened
    
jobs:
  create-issue:
    name: Create Jira issue
    runs-on: ubuntu-latest
    steps:
    - name: Login
      uses: atlassian/gajira-login@v3
      env:
        JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
        JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
        JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}
    
    - name: Create Issue 
      id: create
      uses: atlassian/gajira-create@v3
      with:
        project: DIAR
        issuetype: Subtask
        summary: '${{ github.event.issue.title }}'
        description: '${{ github.event.issue.html_url }} 자동화 테스트 중입니다'
        fields: '{"parent": {"key": "DIAR-38"}}'

    - name: Log created issue
      run: echo "Jira Issue DIAR-38/${{ steps.create.outputs.issue }} was created"

    - name: Create Ticket Number Branch
      uses: peterjgrainger/action-create-branch@v2.0.1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        branch: ${{ steps.create.outputs.issue }}

    - name: Update issue title
      uses: actions-cool/issues-helper@v3
      with:
        actions: 'update-issue'
        token: ${{ secrets.GITHUB_TOKEN }}
        title: '${{ steps.create.outputs.issue }} ${{ github.event.issue.title }}'
